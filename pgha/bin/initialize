#!/usr/bin/env bash

if variable is empty my_ip
then log error "my_ip must be set with configure in order to initialize pgha."
fi

for tag in ${tags[@]}
do
	case ${tag} in
		(master)                 
			etcd_ctl set pgha/master/ip "${my_ip}"
			;;
	esac
done


